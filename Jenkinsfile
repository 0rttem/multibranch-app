pipeline {
    agent any

    stages {
        stage('Calculate') {
            steps {
                script {
                    def result = 1 + 1
                    echo "The result of 1 + 1 is: ${result}"
                }
            }
        }
    }
}
